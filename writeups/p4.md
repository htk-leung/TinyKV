4A:
- layer of abstraction
- ref/comapare standalone storage

4B :
- error handling - need to check what errors are returned and attach them to response
- conditions when getting lock:
    acquire latch : check if latches for this server are available (local)
    GetLock() : check if lock for req keys are available in the whole cluster (global)
    MostRecentWrite() : check if this txn is behind others for certain keys
- how to prewrite, how to commit

4C
- need to be aware of time units, ttl and TS are encoded differently
    ttl in MILLISECONDS, but TS are in MICROSECONDS
- deviation from paper : primary key not used as synchronization point

OVERALL:
- codebase structure
- architecture
- progression
- explain (mvcc & snapshot isolation) X (code)